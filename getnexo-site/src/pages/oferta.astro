 ---
title: "Oferta Especial GetNexo – Promoção Limitada"
description: "Aproveite nossa oferta especial com desconto exclusivo. Automação WhatsApp completa por tempo limitado."
keywords: "oferta getnexo, promocao whatsapp ia, desconto automacao, oferta limitada"
---

<div class="offer-page">
  <div class="container">
    <div class="offer-header">
      <div class="badge">🔥 OFERTA LIMITADA</div>
      <h1>Oferta Especial GetNexo</h1>
      <p class="subtitle">Economia de até 70% nos primeiros 6 meses</p>
      <div class="timer">
        <span id="days">00</span>:<span id="hours">00</span>:<span id="minutes">00</span>:<span id="seconds">00</span>
      </div>
    </div>

    <div class="offer-content">
      <div class="pricing-card">
        <div class="plan-header">
          <h2>Plano Professional</h2>
          <div class="original-price">De R$ 497/mês</div>
          <div class="offer-price">
            <span class="price">R$ 147</span>
            <span class="period">/mês</span>
          </div>
          <div class="savings">Economia de R$ 350/mês</div>
        </div>

        <div class="plan-features">
          <div class="feature">✅ Chatbot IA 24/7</div>
          <div class="feature">✅ Magic Replies GPT-4</div>
          <div class="feature">✅ Sistema de Tickets</div>
          <div class="feature">✅ Gamificação de Vendas</div>
          <div class="feature">✅ Analytics em Tempo Real</div>
          <div class="feature">✅ Suporte Prioritário</div>
          <div class="feature">✅ Setup Gratuito</div>
        </div>

        <div class="cta-section">
          <a href="/criar-bot?plano=oferta" class="btn-primary-glow">GARANTIR OFERTA AGORA</a>
          <p class="guarantee">🛡️ Garantia de 30 dias</p>
        </div>
      </div>

      <div class="testimonials">
        <h3>O que nossos clientes dizem</h3>
        <div class="testimonial">
          <div class="stars">⭐⭐⭐⭐⭐</div>
          <p>"A oferta foi perfeita para começar. Já recuperei o investimento em 2 semanas!"</p>
          <cite>- João Silva, E-commerce</cite>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .offer-page {
    padding: 4rem 5%;
    background: linear-gradient(135deg, #030712 0%, #0f172a 100%);
    color: white;
    min-height: 100vh;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .offer-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .badge {
    background: linear-gradient(135deg, #ff6b35, #f7931e);
    color: white;
    padding: 0.5rem 1.5rem;
    border-radius: 50px;
    font-weight: bold;
    display: inline-block;
    margin-bottom: 1rem;
    animation: pulse 2s infinite;
  }

  h1 {
    font-size: 3rem;
    font-weight: 800;
    margin-bottom: 1rem;
    text-shadow: 0 0 30px rgba(0, 212, 255, 0.3);
  }

  .subtitle {
    font-size: 1.3rem;
    color: #94a3b8;
    margin-bottom: 2rem;
  }

  .timer {
    font-size: 2rem;
    font-weight: bold;
    color: #00d4ff;
    font-family: 'Courier New', monospace;
    margin-top: 2rem;
  }

  .pricing-card {
    background: rgba(15, 23, 42, 0.8);
    border: 1px solid rgba(0, 212, 255, 0.3);
    border-radius: 20px;
    padding: 3rem;
    text-align: center;
    margin-bottom: 3rem;
    box-shadow: 0 20px 40px rgba(0, 212, 255, 0.1);
  }

  .plan-header h2 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: #00d4ff;
  }

  .original-price {
    text-decoration: line-through;
    color: #94a3b8;
    font-size: 1.2rem;
  }

  .offer-price {
    font-size: 3rem;
    font-weight: bold;
    color: #00ff9d;
    margin: 1rem 0;
  }

  .price {
    font-size: 4rem;
  }

  .savings {
    background: #ff6b35;
    color: white;
    padding: 0.5rem 1rem;
    border-radius: 10px;
    font-weight: bold;
    display: inline-block;
    margin-top: 1rem;
  }

  .plan-features {
    margin: 2rem 0;
    text-align: left;
    max-width: 400px;
    margin-left: auto;
    margin-right: auto;
  }

  .feature {
    padding: 0.5rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    font-size: 1.1rem;
  }

  .feature:last-child {
    border-bottom: none;
  }

  .btn-primary-glow {
    background: linear-gradient(135deg, #00d4ff, #00ff9d);
    color: black;
    padding: 1.5rem 3rem;
    border-radius: 15px;
    font-weight: 800;
    text-decoration: none;
    font-size: 1.3rem;
    display: inline-block;
    transition: transform 0.3s;
    box-shadow: 0 10px 30px rgba(0, 212, 255, 0.3);
  }

  .btn-primary-glow:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 40px rgba(0, 212, 255, 0.4);
  }

  .guarantee {
    margin-top: 1rem;
    color: #00ff9d;
    font-weight: 600;
  }

  .testimonials {
    margin-top: 3rem;
  }

  .testimonials h3 {
    text-align: center;
    font-size: 1.8rem;
    margin-bottom: 2rem;
    color: #00d4ff;
  }

  .testimonial {
    background: rgba(15, 23, 42, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 15px;
    padding: 2rem;
    text-align: center;
    max-width: 500px;
    margin: 0 auto;
  }

  .stars {
    font-size: 1.5rem;
    margin-bottom: 1rem;
  }

  .testimonial p {
    color: #e2e8f0;
    font-style: italic;
    margin-bottom: 1rem;
  }

  cite {
    color: #94a3b8;
    font-weight: 600;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.7; }
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2.5rem;
    }

    .pricing-card {
      padding: 2rem;
    }

    .offer-price {
      font-size: 2.5rem;
    }

    .price {
      font-size: 3rem;
    }
  }
</style>

<script nonce={Astro.locals.nonce}>
  // Countdown Timer
  function updateTimer() {
    const endDate = new Date(Date.now() + 7 * 24 * 60 * 60 * 1000); // 7 days from now
    const now = new Date();
    const diff = endDate - now;

    if (diff > 0) {
      const days = Math.floor(diff / (1000 * 60 * 60 * 24));
      const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));
      const minutes = Math.floor((diff % (1000 * 60 * 60)) / (1000 * 60));
      const seconds = Math.floor((diff % (1000 * 60)) / 1000);

      document.getElementById('days').textContent = days.toString().padStart(2, '0');
      document.getElementById('hours').textContent = hours.toString().padStart(2, '0');
      document.getElementById('minutes').textContent = minutes.toString().padStart(2, '0');
      document.getElementById('seconds').textContent = seconds.toString().padStart(2, '0');
    }
  }

  setInterval(updateTimer, 1000);
  updateTimer();
</script>
