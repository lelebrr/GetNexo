import { f as createComponent, k as renderComponent, r as renderTemplate, h as addAttribute, m as maybeRenderHead } from "../assets/astro/server-MCYX8tFF.js";
import "piccolore";
import { $ as $$Layout } from "../assets/Layout-DScI-qCd.js";
/* empty css                               */
import { renderers } from "../renderers.mjs";
var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Demo = createComponent(async ($$result, $$props, $$slots) => {
  const title = "Demo Grátis WhatsApp IA GetNexo - Teste Assistente de Vendas Inteligente Agora Mesmo 2026";
  const description = "Teste grátis a demo interativa do GetNexo: converse com IA de vendas WhatsApp, veja automação em ação, recuperação de carrinho, PIX no chat. Sem instalação, 100% gratuita!";
  const keywords = "demo whatsapp ia getnexo gratis, teste assistente vendas whatsapp, demo automacao whatsapp vendas, ia vendas whatsapp demo, chat inteligente whatsapp demo, recuperacao carrinho demo, pix whatsapp demo gratis, getnexo demo interativa 2026";
  const ogImage = "https://getnexo.com.br/assets/og-poster.jpg";
  return renderTemplate`${renderComponent($$result, "Layout", $$Layout, { "title": title, "description": description, "keywords": keywords, "ogImage": ogImage, "data-astro-cid-prwxh6m4": true }, { "default": async ($$result2) => renderTemplate(_a || (_a = __template([" ", '<section style="padding: 6rem 10%; max-width: 1200px; margin: 0 auto; color: #e5e7eb; text-align: center;" data-astro-cid-prwxh6m4> <h1 style="color:#00ff9d; font-size: 3rem;" data-astro-cid-prwxh6m4>Demonstração Interativa</h1> <p style="font-size: 1.3rem; margin: 1rem 0 3rem; color: #94a3b8;" data-astro-cid-prwxh6m4>Veja o GetNexo em ação – converse com nossa IA agora mesmo</p> <!-- Stage 1: Lead Form --> <div id="demo-stage-1" style="background: rgba(20,30,50,0.8); border-radius: 16px; padding: 3rem; border: 1px solid #00d4ff55; max-width: 500px; margin: 0 auto; text-align: left;" data-astro-cid-prwxh6m4> <h3 style="color: #00ff9d; margin-bottom: 1rem; text-align: center;" data-astro-cid-prwxh6m4>🚀 Testar Atendimento IA</h3> <p style="color: #94a3b8; text-align: center; margin-bottom: 2rem;" data-astro-cid-prwxh6m4>Preencha para iniciar a demonstração</p> <form id="lead-form" style="display: flex; flex-direction: column; gap: 1rem;" data-astro-cid-prwxh6m4> <input type="text" id="lead-name" placeholder="Seu Nome" required style="padding: 1rem; background: #0f172a; border: 1px solid #334155; color: white; border-radius: 8px;" data-astro-cid-prwxh6m4> <input type="tel" id="lead-phone" placeholder="Seu WhatsApp (99) 99999-9999" required maxlength="15" maxlength="15" style="padding: 1rem; background: #0f172a; border: 1px solid #334155; color: white; border-radius: 8px;" data-astro-cid-prwxh6m4> <input type="email" id="lead-email" placeholder="Seu E-mail Corporativo" required style="padding: 1rem; background: #0f172a; border: 1px solid #334155; color: white; border-radius: 8px;" data-astro-cid-prwxh6m4> <button type="submit" style="background: linear-gradient(90deg, #00d4ff, #00ff9d); color: black; padding: 1rem; border-radius: 8px; font-weight: bold; cursor: pointer; border: none; margin-top: 1rem;" data-astro-cid-prwxh6m4>Continuar →</button> </form> </div> <!-- Stage 2: Industry Selection --> <div id="demo-stage-2" style="display: none; background: rgba(20,30,50,0.8); border-radius: 16px; padding: 3rem; border: 1px solid #00d4ff55; max-width: 800px; margin: 0 auto;" data-astro-cid-prwxh6m4> <h3 style="color: #00d4ff; margin-bottom: 2rem;" data-astro-cid-prwxh6m4>🏢 Qual é o seu segmento?</h3> <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 1rem;" data-astro-cid-prwxh6m4> ', ` </div> </div> <!-- Stage 3: Chat --> <div id="demo-stage-3" style="display: none; background: rgba(20,30,50,0.8); border-radius: 16px; padding: 3rem; border: 1px solid #00d4ff55; max-width: 600px; margin: 0 auto;" data-astro-cid-prwxh6m4> <div style="text-align: left; margin-bottom: 2rem;" data-astro-cid-prwxh6m4> <h3 style="color: #00d4ff; margin-bottom: 0.5rem;" id="chat-title" data-astro-cid-prwxh6m4>💬 Chat Inteligente</h3> <p style="color: #94a3b8; font-size: 0.9rem;" data-astro-cid-prwxh6m4>A IA vai simular um vendedor especialista no seu nicho.</p> </div> <div id="chat-messages" style="background: #0a0e17; border-radius: 12px; padding: 1.5rem; min-height: 300px; max-height: 400px; overflow-y: auto; margin-bottom: 1.5rem; text-align: left;" data-astro-cid-prwxh6m4> <div class="msg-bot" data-astro-cid-prwxh6m4> <span style="color: #00ff9d; font-size: 0.8rem;" data-astro-cid-prwxh6m4>🤖 GetNexo Bot</span> <p style="background: #1e293b; padding: 0.8rem; border-radius: 8px; margin: 0.3rem 0 1rem; color: white;" data-astro-cid-prwxh6m4>
Olá! Vi que você trabalha com <b id="chat-industry-display" data-astro-cid-prwxh6m4>...</b>. 
            Como posso otimizar suas vendas hoje? Quer ver uma automação de recuperação de carrinho ou atendimento de suporte?
</p> </div> </div> <form id="chat-form" style="display: flex; gap: 0.5rem; align-items: center;" data-astro-cid-prwxh6m4> <input type="text" id="chat-input" placeholder="Digite ou fale..." style="flex: 1; padding: 1rem; border-radius: 8px; background: #1e293b; color: white; border: 1px solid #334155; font-size: 1rem;" data-astro-cid-prwxh6m4> <!-- Mic Button --> <button type="button" id="btn-mic" onclick="toggleMic()" style="background: #334155; color: white; width: 50px; height: 50px; border-radius: 50%; border: none; cursor: pointer; display: flex; align-items: center; justify-content: center; font-size: 1.2rem; transition: all 0.2s;" data-astro-cid-prwxh6m4>
🎤
</button> <button type="submit" style="background: linear-gradient(90deg, #00d4ff, #00ff9d); color: black; padding: 0 1.5rem; height: 50px; border: none; border-radius: 8px; font-weight: bold; cursor: pointer;" data-astro-cid-prwxh6m4>Enviar</button> </form> </div> </section>  <script>
    // Check if user is admin (logged in)
    const adminToken = localStorage.getItem('adminToken');
    const omnichatToken = localStorage.getItem('omnichat_token');

    if (adminToken || omnichatToken) {
      // Hide or modify demo for logged users
      const welcomeMsg = document.querySelector('.msg-bot');
      if (welcomeMsg) {
        welcomeMsg.style.display = 'none';
      }
      // Maybe add a message saying they're already logged in
      const chatMessages = document.getElementById('chat-messages');
      if (chatMessages) {
        chatMessages.innerHTML = '<div style="text-align:center; color:#64748b; padding:2rem;">Você já está logado como administrador. Acesse o <a href="/admin" style="color:#00d4ff;">painel admin</a> para gerenciar o sistema.</div>';
      }
    }

    const API_URL = window.location.hostname === 'localhost'
      ? 'http://localhost:8080'
      : 'https://api.getnexo.com.br';

    let leadData = {};
    let selectedIndustry = '';

    // Elements
    const stage1 = document.getElementById('demo-stage-1');
    const stage2 = document.getElementById('demo-stage-2');
    const stage3 = document.getElementById('demo-stage-3');
    const chatTitle = document.getElementById('chat-industry-display');

    // Phone formatting
    const phoneInput = document.getElementById('lead-phone');
    if (phoneInput) {
      phoneInput.addEventListener('input', function() {
        let value = this.value.replace(/\\D/g,'');
        this.value = value.replace(/^(\\d{2})(\\d)/g,'($1) $2').replace(/(\\d)(\\d{4})$/,'$1-$2');
      });
    }

    // Stage 1: Lead Form Submit
    document.getElementById('lead-form').onsubmit = (e) => {
      e.preventDefault();
      leadData = {
        name: document.getElementById('lead-name').value,
        phone: document.getElementById('lead-phone').value,
        email: document.getElementById('lead-email').value
      };
      
      // Transition
      stage1.style.display = 'none';
      stage2.style.display = 'block';
    };

    // Stage 2: Select Industry (Global function for inline onclick)
    window.selectIndustry = (industry) => {
      selectedIndustry = industry;
      chatTitle.innerText = industry;
      
      stage2.style.display = 'none';
      stage3.style.display = 'block';

      // Send initial context to backend (optional warmup)
      console.log('Demo started for:', leadData, selectedIndustry);
    };

    // Chat Logic
    const chatMessages = document.getElementById('chat-messages');
    const chatForm = document.getElementById('chat-form');
    const chatInput = document.getElementById('chat-input');
    const btnMic = document.getElementById('btn-mic');

    const scrollToBottom = () => { chatMessages.scrollTop = chatMessages.scrollHeight; };

    function addMessage(text, isBot) {
      const div = document.createElement('div');
      div.style.textAlign = isBot ? 'left' : 'right'; // Align bubbles
      
      // Parse Markdown/Custom Tags to HTML
      let formattedText = text
          // Convert IMAGEM: url to Image + Zoom
          .replace(/IMAGEM:\\s*(https?:\\/\\/[^\\s]+)/g, '<img src="$1" class="chat-car-img" onclick="zoomImage(\\'$1\\')" alt="Veículo">')
          // Convert 360 Link to Button
          .replace(/360:\\s*(https?:\\/\\/[^\\s]+)/g, '<button class="btn-360" onclick="open360(\\'$1\\')">🔄 Girar 360º</button>')
          // Bold
          .replace(/\\*\\*(.*?)\\*\\*/g, '<b>$1</b>')
          // Line breaks
          .replace(/\\n/g, '<br>');

      // TTS Button for Bot
      let ttsBtn = '';
      if (isBot) {
         // Create a cleaner text for speaking (remove URLs)
         const cleanText = text.replace(/IMAGEM:.*?(\\s|$)/g, '').replace(/360:.*?(\\s|$)/g, '').replace(/\\*/g, '');
         ttsBtn = \`<button onclick="speakText(this, '\${cleanText.replace(/'/g, "\\\\'")}')" style="background:none; border:none; color:#00ff9d; cursor:pointer; margin-left:10px; font-size:1.1rem;">🔊</button>\`;
      }

      div.innerHTML = \`
        <div style="display:flex; flex-direction:column; align-items:\${isBot ? 'flex-start' : 'flex-end'};">
            <span style="color: \${isBot ? '#00ff9d' : '#00d4ff'}; font-size: 0.8rem; margin-bottom:4px;">
                \${isBot ? '🤖 GetNexo Bot' : '👤 Você'} \${ttsBtn}
            </span>
            <div style="background: \${isBot ? '#1e293b' : '#00d4ff22'}; padding: 0.8rem; border-radius: 12px; border-top-\${isBot ? 'left' : 'right'}-radius: 0; color: white; display:inline-block; text-align:left; max-width:90%;">
                \${formattedText}
            </div>
        </div>
      \`;
      chatMessages.appendChild(div);
      scrollToBottom();
      
      // Auto-speak if new bot message? (Optional, maybe annoying. Let's keep manual play)
    }
    
    // --- AUDIO LOGIC ---
    let recognition;
    if ('webkitSpeechRecognition' in window) {
        recognition = new webkitSpeechRecognition();
        recognition.continuous = false;
        recognition.lang = 'pt-BR';
        recognition.interimResults = false;

        recognition.onstart = () => {
            btnMic.classList.add('mic-active');
            chatInput.placeholder = "Ouvindo...";
        };
        recognition.onend = () => {
            btnMic.classList.remove('mic-active');
            chatInput.placeholder = "Digite ou fale...";
        };
        recognition.onresult = (event) => {
            const transcript = event.results[0][0].transcript;
            chatInput.value = transcript;
            // Optional: Auto-send
            // chatForm.dispatchEvent(new Event('submit')); 
            // Better to let user review
        };
    } else {
        btnMic.style.display = 'none'; // Hide if not supported
    }

    window.toggleMic = () => {
        if (!recognition) return alert('Seu navegador não suporta reconhecimento de voz.');
        if (btnMic.classList.contains('mic-active')) {
            recognition.stop();
        } else {
            recognition.start();
        }
    };

    window.speakText = (btn, text) => {
        if ('speechSynthesis' in window) {
            // Stop current
            window.speechSynthesis.cancel();
            
            const utterance = new SpeechSynthesisUtterance(text);
            utterance.lang = 'pt-BR';
            utterance.rate = 1.1; // Slightly faster natural speed
            
            // Visual feedback
            const originalIcon = btn.innerHTML;
            btn.innerHTML = '🔈'; // Playing icon
            
            utterance.onend = () => { btn.innerHTML = originalIcon; };
            
            window.speechSynthesis.speak(utterance);
        } else {
            alert('Áudio não suportado neste navegador.');
        }
    };
    // -------------------

    // Lightbox Logic
    window.zoomImage = (src) => {
        const modal = document.createElement('div');
        modal.id = 'img-modal';
        modal.style.cssText = \`position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.95); display:flex; justify-content:center; align-items:center; z-index:9999; cursor:zoom-out; flex-direction:column;\`;
        
        const img = document.createElement('img');
        img.src = src;
        img.style.cssText = "max-width:95%; max-height:85vh; object-fit:contain; border-radius:8px; box-shadow:0 0 50px rgba(0,255,157,0.2);";
        
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = "✕ Fechar Zoom";
        closeBtn.style.cssText = "color:white; margin-top:15px; border:1px solid #777; padding:8px 20px; border-radius:30px; cursor:pointer; font-size:0.9rem; background:rgba(0,0,0,0.5);";
        
        modal.appendChild(img);
        modal.appendChild(closeBtn);
        modal.onclick = () => modal.remove();
        document.body.appendChild(modal);
    };

    window.open360 = (link) => {
        if (!link || link === '[LINK_360]') {
            alert('Visualização 360 não disponível para este modelo.');
            return;
        }

        const modal = document.createElement('div');
        modal.style.cssText = \`position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.95); display:flex; justify-content:center; align-items:center; z-index:9999; flex-direction:column;\`;
        
        modal.innerHTML = \`
            <div style="background:#1e293b; padding:0; border-radius:12px; width:95%; max-width:1100px; height:80vh; display:flex; flex-direction:column; border:1px solid #00d4ff; overflow:hidden;">
                <div style="display:flex; justify-content:space-between; align-items:center; padding:10px 20px; background:#0f172a; border-bottom:1px solid #334155;">
                    <h3 style="color:#00d4ff; margin:0; font-size:1rem;">🔄 CarShow360 Experience</h3>
                    <button onclick="this.closest('div').parentElement.parentElement.remove()" style="background:none; border:none; color:white; font-size:1.5rem; cursor:pointer;">✕</button>
                </div>
                <div style="flex:1; background:#fff; position:relative;">
                    <iframe src="\${link}" style="width:100%; height:100%; border:none;" allowfullscreen loading="lazy"></iframe>
                </div>
            </div>
        \`;
        document.body.appendChild(modal);
    };

    chatForm.onsubmit = async (e) => {
      e.preventDefault();
      const msg = chatInput.value.trim();
      if (!msg) return;

      addMessage(msg, false);
      chatInput.value = '';

      try {
        const res = await fetch(\`\${API_URL}/api/chat\`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            mensagem: msg,
            iaSelecionada: 'openrouter', // Force OpenRouter or dynamic
            model: 'z-ai/glm-4.5-air:free',
            apiKey: '', // Will use backend configured key
            context: {
                industry: selectedIndustry,
                lead: leadData
            }
          })
        });
        const data = await res.json();
        addMessage(data.resposta, true);
      } catch (e) {
         addMessage('Erro de conexão. Tente novamente.', true);
      }
    };
  <\/script>`], [" ", '<section style="padding: 6rem 10%; max-width: 1200px; margin: 0 auto; color: #e5e7eb; text-align: center;" data-astro-cid-prwxh6m4> <h1 style="color:#00ff9d; font-size: 3rem;" data-astro-cid-prwxh6m4>Demonstração Interativa</h1> <p style="font-size: 1.3rem; margin: 1rem 0 3rem; color: #94a3b8;" data-astro-cid-prwxh6m4>Veja o GetNexo em ação – converse com nossa IA agora mesmo</p> <!-- Stage 1: Lead Form --> <div id="demo-stage-1" style="background: rgba(20,30,50,0.8); border-radius: 16px; padding: 3rem; border: 1px solid #00d4ff55; max-width: 500px; margin: 0 auto; text-align: left;" data-astro-cid-prwxh6m4> <h3 style="color: #00ff9d; margin-bottom: 1rem; text-align: center;" data-astro-cid-prwxh6m4>🚀 Testar Atendimento IA</h3> <p style="color: #94a3b8; text-align: center; margin-bottom: 2rem;" data-astro-cid-prwxh6m4>Preencha para iniciar a demonstração</p> <form id="lead-form" style="display: flex; flex-direction: column; gap: 1rem;" data-astro-cid-prwxh6m4> <input type="text" id="lead-name" placeholder="Seu Nome" required style="padding: 1rem; background: #0f172a; border: 1px solid #334155; color: white; border-radius: 8px;" data-astro-cid-prwxh6m4> <input type="tel" id="lead-phone" placeholder="Seu WhatsApp (99) 99999-9999" required maxlength="15" maxlength="15" style="padding: 1rem; background: #0f172a; border: 1px solid #334155; color: white; border-radius: 8px;" data-astro-cid-prwxh6m4> <input type="email" id="lead-email" placeholder="Seu E-mail Corporativo" required style="padding: 1rem; background: #0f172a; border: 1px solid #334155; color: white; border-radius: 8px;" data-astro-cid-prwxh6m4> <button type="submit" style="background: linear-gradient(90deg, #00d4ff, #00ff9d); color: black; padding: 1rem; border-radius: 8px; font-weight: bold; cursor: pointer; border: none; margin-top: 1rem;" data-astro-cid-prwxh6m4>Continuar →</button> </form> </div> <!-- Stage 2: Industry Selection --> <div id="demo-stage-2" style="display: none; background: rgba(20,30,50,0.8); border-radius: 16px; padding: 3rem; border: 1px solid #00d4ff55; max-width: 800px; margin: 0 auto;" data-astro-cid-prwxh6m4> <h3 style="color: #00d4ff; margin-bottom: 2rem;" data-astro-cid-prwxh6m4>🏢 Qual é o seu segmento?</h3> <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 1rem;" data-astro-cid-prwxh6m4> ', ` </div> </div> <!-- Stage 3: Chat --> <div id="demo-stage-3" style="display: none; background: rgba(20,30,50,0.8); border-radius: 16px; padding: 3rem; border: 1px solid #00d4ff55; max-width: 600px; margin: 0 auto;" data-astro-cid-prwxh6m4> <div style="text-align: left; margin-bottom: 2rem;" data-astro-cid-prwxh6m4> <h3 style="color: #00d4ff; margin-bottom: 0.5rem;" id="chat-title" data-astro-cid-prwxh6m4>💬 Chat Inteligente</h3> <p style="color: #94a3b8; font-size: 0.9rem;" data-astro-cid-prwxh6m4>A IA vai simular um vendedor especialista no seu nicho.</p> </div> <div id="chat-messages" style="background: #0a0e17; border-radius: 12px; padding: 1.5rem; min-height: 300px; max-height: 400px; overflow-y: auto; margin-bottom: 1.5rem; text-align: left;" data-astro-cid-prwxh6m4> <div class="msg-bot" data-astro-cid-prwxh6m4> <span style="color: #00ff9d; font-size: 0.8rem;" data-astro-cid-prwxh6m4>🤖 GetNexo Bot</span> <p style="background: #1e293b; padding: 0.8rem; border-radius: 8px; margin: 0.3rem 0 1rem; color: white;" data-astro-cid-prwxh6m4>
Olá! Vi que você trabalha com <b id="chat-industry-display" data-astro-cid-prwxh6m4>...</b>. 
            Como posso otimizar suas vendas hoje? Quer ver uma automação de recuperação de carrinho ou atendimento de suporte?
</p> </div> </div> <form id="chat-form" style="display: flex; gap: 0.5rem; align-items: center;" data-astro-cid-prwxh6m4> <input type="text" id="chat-input" placeholder="Digite ou fale..." style="flex: 1; padding: 1rem; border-radius: 8px; background: #1e293b; color: white; border: 1px solid #334155; font-size: 1rem;" data-astro-cid-prwxh6m4> <!-- Mic Button --> <button type="button" id="btn-mic" onclick="toggleMic()" style="background: #334155; color: white; width: 50px; height: 50px; border-radius: 50%; border: none; cursor: pointer; display: flex; align-items: center; justify-content: center; font-size: 1.2rem; transition: all 0.2s;" data-astro-cid-prwxh6m4>
🎤
</button> <button type="submit" style="background: linear-gradient(90deg, #00d4ff, #00ff9d); color: black; padding: 0 1.5rem; height: 50px; border: none; border-radius: 8px; font-weight: bold; cursor: pointer;" data-astro-cid-prwxh6m4>Enviar</button> </form> </div> </section>  <script>
    // Check if user is admin (logged in)
    const adminToken = localStorage.getItem('adminToken');
    const omnichatToken = localStorage.getItem('omnichat_token');

    if (adminToken || omnichatToken) {
      // Hide or modify demo for logged users
      const welcomeMsg = document.querySelector('.msg-bot');
      if (welcomeMsg) {
        welcomeMsg.style.display = 'none';
      }
      // Maybe add a message saying they're already logged in
      const chatMessages = document.getElementById('chat-messages');
      if (chatMessages) {
        chatMessages.innerHTML = '<div style="text-align:center; color:#64748b; padding:2rem;">Você já está logado como administrador. Acesse o <a href="/admin" style="color:#00d4ff;">painel admin</a> para gerenciar o sistema.</div>';
      }
    }

    const API_URL = window.location.hostname === 'localhost'
      ? 'http://localhost:8080'
      : 'https://api.getnexo.com.br';

    let leadData = {};
    let selectedIndustry = '';

    // Elements
    const stage1 = document.getElementById('demo-stage-1');
    const stage2 = document.getElementById('demo-stage-2');
    const stage3 = document.getElementById('demo-stage-3');
    const chatTitle = document.getElementById('chat-industry-display');

    // Phone formatting
    const phoneInput = document.getElementById('lead-phone');
    if (phoneInput) {
      phoneInput.addEventListener('input', function() {
        let value = this.value.replace(/\\\\D/g,'');
        this.value = value.replace(/^(\\\\d{2})(\\\\d)/g,'($1) $2').replace(/(\\\\d)(\\\\d{4})$/,'$1-$2');
      });
    }

    // Stage 1: Lead Form Submit
    document.getElementById('lead-form').onsubmit = (e) => {
      e.preventDefault();
      leadData = {
        name: document.getElementById('lead-name').value,
        phone: document.getElementById('lead-phone').value,
        email: document.getElementById('lead-email').value
      };
      
      // Transition
      stage1.style.display = 'none';
      stage2.style.display = 'block';
    };

    // Stage 2: Select Industry (Global function for inline onclick)
    window.selectIndustry = (industry) => {
      selectedIndustry = industry;
      chatTitle.innerText = industry;
      
      stage2.style.display = 'none';
      stage3.style.display = 'block';

      // Send initial context to backend (optional warmup)
      console.log('Demo started for:', leadData, selectedIndustry);
    };

    // Chat Logic
    const chatMessages = document.getElementById('chat-messages');
    const chatForm = document.getElementById('chat-form');
    const chatInput = document.getElementById('chat-input');
    const btnMic = document.getElementById('btn-mic');

    const scrollToBottom = () => { chatMessages.scrollTop = chatMessages.scrollHeight; };

    function addMessage(text, isBot) {
      const div = document.createElement('div');
      div.style.textAlign = isBot ? 'left' : 'right'; // Align bubbles
      
      // Parse Markdown/Custom Tags to HTML
      let formattedText = text
          // Convert IMAGEM: url to Image + Zoom
          .replace(/IMAGEM:\\\\s*(https?:\\\\/\\\\/[^\\\\s]+)/g, '<img src="$1" class="chat-car-img" onclick="zoomImage(\\\\'$1\\\\')" alt="Veículo">')
          // Convert 360 Link to Button
          .replace(/360:\\\\s*(https?:\\\\/\\\\/[^\\\\s]+)/g, '<button class="btn-360" onclick="open360(\\\\'$1\\\\')">🔄 Girar 360º</button>')
          // Bold
          .replace(/\\\\*\\\\*(.*?)\\\\*\\\\*/g, '<b>$1</b>')
          // Line breaks
          .replace(/\\\\n/g, '<br>');

      // TTS Button for Bot
      let ttsBtn = '';
      if (isBot) {
         // Create a cleaner text for speaking (remove URLs)
         const cleanText = text.replace(/IMAGEM:.*?(\\\\s|$)/g, '').replace(/360:.*?(\\\\s|$)/g, '').replace(/\\\\*/g, '');
         ttsBtn = \\\`<button onclick="speakText(this, '\\\${cleanText.replace(/'/g, "\\\\\\\\'")}')" style="background:none; border:none; color:#00ff9d; cursor:pointer; margin-left:10px; font-size:1.1rem;">🔊</button>\\\`;
      }

      div.innerHTML = \\\`
        <div style="display:flex; flex-direction:column; align-items:\\\${isBot ? 'flex-start' : 'flex-end'};">
            <span style="color: \\\${isBot ? '#00ff9d' : '#00d4ff'}; font-size: 0.8rem; margin-bottom:4px;">
                \\\${isBot ? '🤖 GetNexo Bot' : '👤 Você'} \\\${ttsBtn}
            </span>
            <div style="background: \\\${isBot ? '#1e293b' : '#00d4ff22'}; padding: 0.8rem; border-radius: 12px; border-top-\\\${isBot ? 'left' : 'right'}-radius: 0; color: white; display:inline-block; text-align:left; max-width:90%;">
                \\\${formattedText}
            </div>
        </div>
      \\\`;
      chatMessages.appendChild(div);
      scrollToBottom();
      
      // Auto-speak if new bot message? (Optional, maybe annoying. Let's keep manual play)
    }
    
    // --- AUDIO LOGIC ---
    let recognition;
    if ('webkitSpeechRecognition' in window) {
        recognition = new webkitSpeechRecognition();
        recognition.continuous = false;
        recognition.lang = 'pt-BR';
        recognition.interimResults = false;

        recognition.onstart = () => {
            btnMic.classList.add('mic-active');
            chatInput.placeholder = "Ouvindo...";
        };
        recognition.onend = () => {
            btnMic.classList.remove('mic-active');
            chatInput.placeholder = "Digite ou fale...";
        };
        recognition.onresult = (event) => {
            const transcript = event.results[0][0].transcript;
            chatInput.value = transcript;
            // Optional: Auto-send
            // chatForm.dispatchEvent(new Event('submit')); 
            // Better to let user review
        };
    } else {
        btnMic.style.display = 'none'; // Hide if not supported
    }

    window.toggleMic = () => {
        if (!recognition) return alert('Seu navegador não suporta reconhecimento de voz.');
        if (btnMic.classList.contains('mic-active')) {
            recognition.stop();
        } else {
            recognition.start();
        }
    };

    window.speakText = (btn, text) => {
        if ('speechSynthesis' in window) {
            // Stop current
            window.speechSynthesis.cancel();
            
            const utterance = new SpeechSynthesisUtterance(text);
            utterance.lang = 'pt-BR';
            utterance.rate = 1.1; // Slightly faster natural speed
            
            // Visual feedback
            const originalIcon = btn.innerHTML;
            btn.innerHTML = '🔈'; // Playing icon
            
            utterance.onend = () => { btn.innerHTML = originalIcon; };
            
            window.speechSynthesis.speak(utterance);
        } else {
            alert('Áudio não suportado neste navegador.');
        }
    };
    // -------------------

    // Lightbox Logic
    window.zoomImage = (src) => {
        const modal = document.createElement('div');
        modal.id = 'img-modal';
        modal.style.cssText = \\\`position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.95); display:flex; justify-content:center; align-items:center; z-index:9999; cursor:zoom-out; flex-direction:column;\\\`;
        
        const img = document.createElement('img');
        img.src = src;
        img.style.cssText = "max-width:95%; max-height:85vh; object-fit:contain; border-radius:8px; box-shadow:0 0 50px rgba(0,255,157,0.2);";
        
        const closeBtn = document.createElement('div');
        closeBtn.innerHTML = "✕ Fechar Zoom";
        closeBtn.style.cssText = "color:white; margin-top:15px; border:1px solid #777; padding:8px 20px; border-radius:30px; cursor:pointer; font-size:0.9rem; background:rgba(0,0,0,0.5);";
        
        modal.appendChild(img);
        modal.appendChild(closeBtn);
        modal.onclick = () => modal.remove();
        document.body.appendChild(modal);
    };

    window.open360 = (link) => {
        if (!link || link === '[LINK_360]') {
            alert('Visualização 360 não disponível para este modelo.');
            return;
        }

        const modal = document.createElement('div');
        modal.style.cssText = \\\`position:fixed; top:0; left:0; width:100%; height:100%; background:rgba(0,0,0,0.95); display:flex; justify-content:center; align-items:center; z-index:9999; flex-direction:column;\\\`;
        
        modal.innerHTML = \\\`
            <div style="background:#1e293b; padding:0; border-radius:12px; width:95%; max-width:1100px; height:80vh; display:flex; flex-direction:column; border:1px solid #00d4ff; overflow:hidden;">
                <div style="display:flex; justify-content:space-between; align-items:center; padding:10px 20px; background:#0f172a; border-bottom:1px solid #334155;">
                    <h3 style="color:#00d4ff; margin:0; font-size:1rem;">🔄 CarShow360 Experience</h3>
                    <button onclick="this.closest('div').parentElement.parentElement.remove()" style="background:none; border:none; color:white; font-size:1.5rem; cursor:pointer;">✕</button>
                </div>
                <div style="flex:1; background:#fff; position:relative;">
                    <iframe src="\\\${link}" style="width:100%; height:100%; border:none;" allowfullscreen loading="lazy"></iframe>
                </div>
            </div>
        \\\`;
        document.body.appendChild(modal);
    };

    chatForm.onsubmit = async (e) => {
      e.preventDefault();
      const msg = chatInput.value.trim();
      if (!msg) return;

      addMessage(msg, false);
      chatInput.value = '';

      try {
        const res = await fetch(\\\`\\\${API_URL}/api/chat\\\`, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            mensagem: msg,
            iaSelecionada: 'openrouter', // Force OpenRouter or dynamic
            model: 'z-ai/glm-4.5-air:free',
            apiKey: '', // Will use backend configured key
            context: {
                industry: selectedIndustry,
                lead: leadData
            }
          })
        });
        const data = await res.json();
        addMessage(data.resposta, true);
      } catch (e) {
         addMessage('Erro de conexão. Tente novamente.', true);
      }
    };
  <\/script>`])), maybeRenderHead(), ["Moda & Vestuário", "Eletrônicos", "Auto Peças", "Cosméticos", "Alimentação/Delivery", "Móveis & Decoração", "Pet Shop", "Serviços/Infoproduto", "Imobiliária", "Concessionária", "Outros"].map((item) => renderTemplate`<button class="industry-btn"${addAttribute(`selectIndustry('${item}')`, "onclick")} style="padding: 1rem; background: #1e293b; border: 1px solid #334155; color: white; border-radius: 8px; cursor: pointer; transition: all 0.3s;" data-astro-cid-prwxh6m4>${item}</button>`)) })}`;
}, "/home/lele/usenexo/getnexo-site/src/pages/demo.astro", void 0);
const $$file = "/home/lele/usenexo/getnexo-site/src/pages/demo.astro";
const $$url = "/demo";
const _page = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  default: $$Demo,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: "Module" }));
const page = () => _page;
export {
  page,
  renderers
};
